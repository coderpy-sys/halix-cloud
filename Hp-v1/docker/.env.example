# Optional — copy to docker/.env and adjust. Used by install/halix-install.sh.
# If this file is absent, docker-compose.yml defaults apply.

HALIX_DB_USER=halix
HALIX_DB_PASSWORD=halix_dev_change_me
HALIX_DB_NAME=halix
HALIX_PG_PORT=5432
HALIX_REDIS_PORT=6379
